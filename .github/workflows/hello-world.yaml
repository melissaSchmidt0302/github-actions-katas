name: hello-world
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Test
        run: ci/unit-test-app.sh
      - name: my-step
        run: ci/build-app.sh
    container: gradle:6-jdk11